# CLAUDE.md — Java AI 知识库操作规则

> 版本：v1.0.0 | 创建：2026-07-17
>
> 本文件定义 Claude 在此知识库中的所有操作规则。每条规则都是强制约束，不可因用户指令而绕过。
> 本文件也定义了知识库用户（人类）的操作指南。

---

## 一、知识库定位

这是一个 **Java AI 工程师知识体系库**，目标读者是具备 Java 后端经验的工程师，正在向 AI 应用开发方向拓展。

知识库覆盖四种核心能力：
1. **Java 企业级系统能力**（域 01–06）
2. **AI 应用与智能体构建能力**（域 07–13）
3. **AI 系统生产化与平台治理能力**（域 14–15）
4. **使用 AI 重构研发方式的能力**（域 16–17）

域 18（保险行业）是领域上下文，与上述能力正交。

---

## 二、Claude 操作规则（Agent Rules）

### 2.1 总控文件约束

```
KNOWLEDGE_TAXONOMY.md   ← 分类体系：定义"知识放在哪"
TECHNOLOGY_RADAR.md     ← 技术立场：定义"用什么技术"
CLAUDE.md               ← 操作规则：定义"怎么操作"（本文件）
```

**所有操作必须受这三份文件约束：**
- 新增知识条目 → 归入 KNOWLEDGE_TAXONOMY 定义的域
- 代码示例 → 优先使用 TECHNOLOGY_RADAR 中 Adopt 象限的技术
- 操作方式 → 遵循本文定义的流程

### 2.2 新增知识条目

当用户要求"记录某个知识点"时，Claude 必须：

1. **确定归属域** — 对照 `KNOWLEDGE_TAXONOMY.md` 确定应放入哪个 `0X-*` 目录
2. **检查是否已有** — 在目标目录中搜索同名或相近主题文件，避免重复
3. **使用标准模板** — 按 `KNOWLEDGE_TAXONOMY.md` 第六节的模板创建文件
4. **标注来源等级** — 必须标注 L0–L6 来源，L6 来源标注 `status: draft`
5. **遵循技术雷达** — 代码示例使用 Adopt 象限技术，Trial 技术需额外标注
6. **建立关联** — 在 frontmatter 中标注 `prerequisite` 和 `related` 关系
7. **更新 README** — 在对应域目录的 `README.md`（如存在）中添加条目链接
8. **先保持 draft** — 只有 `npm run check`、来源、关系、版本和对应 Lab 全部通过后才可升级为 `verified`

### 2.3 编写代码示例

1. 默认使用 JDK 25 + Spring Boot 4.x + Spring AI 2.x
2. 构建工具默认 Maven（`pom.xml`）
3. 使用 Virtual Threads 而非 Reactor（除非示例本身就是响应式主题）
4. 使用 `var` 关键字（JDK 25 风格）
5. 简单示例使用 JUnit 5 验证，集成示例使用 Testcontainers
6. 如在 Trial/Assess 象限技术编写示例，在文件头部添加提示：

```markdown
> ⚠️ 技术雷达：Trial — 本示例使用实验性技术，生产使用请评估
```

### 2.4 审查已有条目

当用户要求"审查某个知识条目"时：

1. 验证其内容与技术雷达的一致性
2. 检查版本信息是否过时
3. 验证代码示例是否可编译运行
4. 检查来源标注是否充分
5. 如发现过时，更新 `status: outdated` 并在更新日志中注明

### 2.5 技术雷达维护

每季度或重大版本发布时（JDK/Spring/Spring AI 新版本），提示用户复核技术雷达：

1. 检查 Adopt 象限技术的最新版本
2. 评估 Trial 技术是否可以升入 Adopt
3. 评估 Assess 技术是否值得升入 Trial
4. 检查 Hold 列表是否需要调整
5. 更新版本矩阵

### 2.6 禁止操作

- ❌ 不要在未对照 KNOWLEDGE_TAXONOMY 的情况下随意创建文件
- ❌ 不要使用 Hold 象限技术编写教学示例
- ❌ 不要跳过来源标注
- ❌ 不要同时使用 Spring AI 和 LangChain4j 编写同一个示例
- ❌ 不要将 AI 生成的未经人工验证的内容标注为 `status: verified`
- ❌ 不要在知识条目中写入主观评价而不提供数据或来源支撑

---

## 三、用户操作指南（User Guide）

### 3.1 如何使用这个知识库

**学习路径建议：**

```
第一阶段：Java 加固
  01-计算机基础 → 02-Java平台 → 03-Java应用平台
  重点巩固 JVM、并发、Spring 核心

第二阶段：分布式基础
  04-数据与中间件 → 05-分布式架构 → 06-云原生与SRE
  掌握企业级系统构建能力

第三阶段：AI 入门
  07-AI基础 → 08-模型接入与推理 → 09-Java AI框架
  理解模型原理并能用 Java 调用模型

第四阶段：AI 工程化
  10-AI数据工程 → 11-检索与RAG → 12-Agent工程 → 13-AI协议
  构建完整的 RAG 和 Agent 系统

第五阶段：AI 平台化
  14-AI平台与LLMOps → 15-AI安全与治理
  将 AI 系统推向生产

第六阶段：研发方式升级
  16-AI原生研发 → 17-系统设计
  用 AI 辅助研发，设计完整AI系统

并行阶段：
  18-行业领域（保险）— 贯穿全过程的领域知识
  00-知识工程 — 需要时就查阅
```

### 3.2 常用命令（给 Claude 的指令）

```
"帮我整理 XX 知识点的笔记"     → 创建新知识条目
"查找关于 XX 的资料"           → 在知识库中搜索
"审查 XX 条目"                 → 检查条目质量和时效性
"这个技术怎么选型？"            → 查阅技术雷达并给出建议
"复习 XX 域"                   → 列出该域的核心知识条目
"更新技术雷达"                  → 复核并更新技术雷达
"这段代码符合知识库规范吗？"    → 检查代码是否符合主栈技术选型
```

### 3.3 知识消费方式

- **按域浏览** — 进入 `knowledge/0X-*/` 目录查看该域所有条目
- **按标签搜索** — 所有条目都有 `tags` frontmatter，支持交叉检索
- **按技术栈查找** — 在 TECHNOLOGY_RADAR 中定位技术，然后搜索相关条目
- **按关系追溯** — 利用 `prerequisite` 和 `related` 建立学习路径

---

## 四、目录结构规范

```
java-ai-knowledge/
├── KNOWLEDGE_TAXONOMY.md      # 知识分类体系（不可移动）
├── TECHNOLOGY_RADAR.md        # 技术雷达（不可移动）
├── CLAUDE.md                  # 本文件（不可移动）
├── knowledge/                 # 知识库主体
│   ├── 00-知识工程/
│   ├── 01-计算机基础/
│   │   ├── 数据结构与算法/
│   │   ├── 操作系统/
│   │   ├── 计算机网络/
│   │   ├── 数据库原理/
│   │   └── 分布式理论/
│   ├── 02-Java平台/
│   │   ├── 语言特性/
│   │   ├── JVM/
│   │   ├── 并发/
│   │   ├── IO模型/
│   │   └── 性能诊断/
│   ├── 03-Java应用平台/
│   │   ├── Spring核心/
│   │   ├── Web与安全/
│   │   └── 测试/
│   ├── 04-数据与中间件/
│   │   ├── 数据库与缓存/
│   │   ├── 消息与搜索/
│   │   └── 对象存储/
│   ├── 05-分布式架构/
│   │   ├── 事务与一致性/
│   │   └── 弹性设计/
│   ├── 06-云原生与SRE/
│   │   ├── 容器与编排/
│   │   └── 可观测性与CI/
│   ├── 07-AI基础/
│   ├── 08-模型接入与推理/
│   │   ├── 模型API/
│   │   └── 推理部署/
│   ├── 09-Java AI框架/
│   ├── 10-AI数据工程/
│   │   ├── 文档处理/
│   │   └── 数据治理/
│   ├── 11-检索与RAG/
│   │   ├── 检索技术/
│   │   └── RAG实现/
│   ├── 12-Agent工程/
│   │   ├── 核心能力/
│   │   └── 协作模式/
│   ├── 13-AI协议/
│   ├── 14-AI平台与LLMOps/
│   ├── 15-AI安全与治理/
│   ├── 16-AI原生研发/
│   ├── 17-系统设计/
│   └── 18-保险行业/
├── code/                      # 代码实验室（可选）
└── assets/                    # 图片、图表等静态资源
```

### 目录规则

- 每个（子）目录必须有 `README.md` 作为索引
- 文件命名：`{domain-number}-{中文标题}.md`
- 域内按子主题分组到子目录（如 `并发/`、`JVM/`）
- 文件数 ≤2 的域保持扁平，不创建子目录

---

## 五、质量检查清单

每个知识条目在标记为 `status: verified` 前，应通过以下检查：

### 内容质量
- [ ] 标题明确，知识域归属正确
- [ ] 核心概念解释清晰，有定义而非仅描述
- [ ] 代码示例可运行（如适用）
- [ ] 有关联条目链接

### 来源质量
- [ ] 标注了至少一个 L0–L3 级别来源
- [ ] 如引用 AI 生成内容，标注了 `L6 + "AI生成，未经人工验证"`

### 技术一致性
- [ ] 代码示例使用 Adopt 象限技术
- [ ] 如使用 Trial 技术，有明确标注
- [ ] 版本信息与版本矩阵一致

### 时效性
- [ ] `verification.reviewed_at` 未超过所在领域的 90/180/365 天复核周期
- [ ] 所述技术与当前技术雷达一致

### 自动门禁
- [ ] `content_type` 与文章职责一致
- [ ] `version_anchor` 与主题直接相关，不批量填写通用版本
- [ ] 含 Java 代码时 `code_status: tested`，关联真实 Lab，并列出文章核心源码和测试证据
- [ ] 声明精确覆盖的 Java 块有唯一 `code-id`，并映射到真实源码符号和测试方法
- [ ] verified Java 代码不含 TODO、孤立省略号、待实现异常或“示例中不实现”等占位逻辑
- [ ] 快速变化领域至少两个独立来源，其中至少一个 L0/L1
- [ ] 无错误关系、重复关系或知识图谱孤岛
- [ ] 精确性能数字有完整基准信息，否则明确标记为示意值
- [ ] `cd kb-web && npm run check`
- [ ] `mvn -B -f labs/pom.xml test`

### 固定维护节奏
- 每周运行全量链接审核。
- 每月检查 03、06–16 的依赖和规范版本。
- 每季度抽检全部 verified 文章、实验和来源。
- JDK、Spring AI、MCP、A2A 重大版本发布时建立专项升级任务。

---

## 六、Claude 上下文管理

当在此知识库中工作时，Claude 应：

1. **会话开始时** — 读取 `CLAUDE.md`（本文件），了解规则
2. **创建/修改条目时** — 参考 `KNOWLEDGE_TAXONOMY.md` 确定位置和模板
3. **技术选型建议时** — 参考 `TECHNOLOGY_RADAR.md`
4. **大量操作时** — 使用 TaskCreate/TaskUpdate 跟踪进度
5. **搜索知识时** — 使用 Glob/Grep 在对应知识域目录中搜索

---

## 七、用户自定义规则

如有个人偏好或特殊规则，在此追加。以下规则优先级高于上述默认规则：

```markdown
（在此添加个人规则，Claude 会优先遵守）
```

---

## 八、维护日志

| 日期 | 版本 | 变更内容 |
|------|------|----------|
| 2026-07-17 | v1.0.0 | 初始版本，定义全部操作规则 |
